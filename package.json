{
  "name": "latrip",
  "version": "1.0.0",
  "private": true,
  "description": "LA Trip Companion 2026, served from a Cloudflare Worker behind a per-user password gate",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "node scripts/deploy.mjs",
    "setup": "bash scripts/setup-secrets.sh",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.130.0"
  }
}
